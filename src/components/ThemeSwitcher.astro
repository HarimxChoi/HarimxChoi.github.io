---
const lang = Astro.props.lang ?? "en";
const label = lang === "ko" ? "색상 모드 전환" : "Toggle color mode";
---

<button id="theme-toggle" class="theme-toggle" type="button" aria-label={label}>
  <span class="theme-toggle-label" data-light>{lang === "ko" ? "라이트" : "Light"}</span>
  <span aria-hidden="true">/</span>
  <span class="theme-toggle-label" data-dark>{lang === "ko" ? "다크" : "Dark"}</span>
</button>

<script is:inline>
  (function () {
    var button = document.getElementById("theme-toggle");
    if (!button) return;
    var sync = function () {
      var dark = document.documentElement.dataset.theme === "dark";
      button.setAttribute("aria-pressed", String(dark));
    };
    sync();
    button.addEventListener("click", function () {
      var next = document.documentElement.dataset.theme === "dark" ? "light" : "dark";
      document.documentElement.dataset.theme = next;
      try { localStorage.setItem("portfolio-theme", next); } catch (error) {}
      sync();
    });
  })();
</script>
